# dsh-iconography · 图标设计规范参考

纯 Node 实现，无网络、无外部服务。

## 安装

```bash
dsh plugin add dsh-iconography
```
安装后在 profile 的 `package.json` 的 `dsh.profile.bundles` 中加入 `"dsh-iconography"`。

## 安装

```bash
dsh plugin add github:uckkk/dsh-iconography
```

> 安装即在本机运行第三方代码，请自行审阅源码。

## 安装

```bash
dsh plugin add github:uckkk/dsh-iconography
```

## 使用

安装后在会话中调用该插件注册的工具即可。

## 许可

MIT

> 安装即在本机运行第三方代码，请自行审阅源码。
